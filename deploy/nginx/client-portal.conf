server {
  listen 80;
  server_name client-portal;
  root /usr/share/nginx/html;

  location / {
    try_files $uri /index.html;
    return 302 /dashboard;
  }

  # Static assets
  location /assets/ {
    access_log off;
    expires 1h;
  }
}
